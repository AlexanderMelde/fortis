import { TrainerCardComponent } from '../../src/app/ui-components/trainer-card/trainer-card.component'

describe('TrainerCardComponent', () => {
  it('should mount', () => {
    cy.mount(TrainerCardComponent)
  })
})
