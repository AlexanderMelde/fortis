import { MediacontrolComponent } from '../../src/app/mediacontrol/mediacontrol.component'

describe('MediacontrolComponent', () => {
  it('should mount', () => {
    cy.mount(MediacontrolComponent)
  })
})
